[run]
relative_files = True
cover_pylib = False

[paths]
source =
    src/
    rssd/

[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    pragma: no cover
    if self\.debug
    if 0:
    if __name__ == .__main__.
